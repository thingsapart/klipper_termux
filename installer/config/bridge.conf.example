# Offline mode publishes the PTY without requiring an attached printer MCU.
# The Android app replaces "offline" with the selected device UUID when available.
server=127.0.0.1
port=27831
token=TOKEN
log_interval=60
device=main,offline,250000,8,1,none,dtr+rts,@PREFIX@/var/run/klipper-android/main
